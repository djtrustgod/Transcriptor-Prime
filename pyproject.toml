[project]
name = "transcriptor-prime"
version = "0.5.0"
description = "Local audio/video transcription GUI with periodic timecodes"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
markers = [
    "slow: runs a real Whisper model; downloads weights on first use",
]
